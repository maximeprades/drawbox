// Copy this file to wifi_credentials.h (gitignored) and fill in your network.
// The ESP32-PICO-D4 has no 5 GHz radio — the network must be 2.4 GHz.
#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-network-password"
