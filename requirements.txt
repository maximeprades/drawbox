# DrawBox runtime dependencies.
# Pinned to recent known-good minimums; the Pi OS Bookworm system Python
# (3.11) handles all of these without a venv.
openai>=1.30,<2
Pillow>=10.1
numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12.1
gpiozero>=2.0
flask>=3.0
gunicorn>=21.2
# Conversation mode (Grok Voice Agent sessions on the Pi box).
# >= 14 is load-bearing: connect(additional_headers=...) only exists in
# the new asyncio client that became the default in 14.0 — on older
# versions (Bookworm apt ships 10.4) the session raises TypeError and
# conversation mode silently never works.
websockets>=14
